#include <stdio.h>

// arithmetic operators + - * / % ++ --

int main(){
/*    
    int x = 2;
    float y = 3;
    float z = 0;

    //z = x + y;
    // z = x - y;
    // z = x * y;
    z = x / y;

    printf("%f", z);
*/
    int x = 10;
    int y = 3;
    int z = 0;

    //z = x % y;
    // x++;
    // x--;
    //x = x + 2;
    //x += 2;
    // x -= 3;
    //x *= 2;
    x /= 2;

    // printf("%d", z);
    printf("%d", x);
    return 0;
}